[tox]
envlist = py34,docs

[testenv]
deps = pytest
       flake8
       coverage
commands =
    coverage run --branch --source=bloop -m py.test
    coverage report -m
    flake8 bloop tests

[testenv:docs]
changedir=docs
deps=sphinx
commands =
    sphinx-build -W -b html -d {envtmpdir}/doctrees .  {envtmpdir}/html
